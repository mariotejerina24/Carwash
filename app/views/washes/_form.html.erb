<%= form_for(@wash) do |f| %>
  <% if @wash.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wash.errors.count, "error") %> prohibited this wash from being saved:</h2>

      <ul>
      <% @wash.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :vehicle_id %><br>
    <%= collection_select(:wash, :vehicle_id, Vehicle.all, :id, :full_nameauto) %>
  </div>
  <div class="field">
    <%= f.label :employee_id %><br>
    <%= collection_select(:wash, :employee_id, Employee.all, :id, :full_name) %>
  </div>
  <div class="field">
    <%= f.label :customer_id %><br>
    <%= collection_select(:wash, :customer_id, Customer.all, :id, :full_name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
